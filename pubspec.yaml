name: Covalt911
description: A new Flutter project.
publish_to: "none"
version: 1.0.0+1

environment:
  sdk: ">=3.1.3 <4.0.0"

dependencies:
  flutter:
    sdk: flutter
   # database storage
  sqflite: ^2.3.0
   # format DateTime
  intl: ^0.18.1
   # display todos in grid
  flutter_staggered_grid_view: ^0.7.0
  path: ^1.8.3
  image_picker: ^0.8.4
  audioplayers: ^5.2.0
  file_picker: ^5.0.4
  url_launcher: ^6.0.13

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^3.0.0
  flutter_launcher_icons: "^0.13.1"

flutter_launcher_icons:
  android: "launcher_icon"
  ios: true
  image_path: "assets/war_icon.png"

flutter:
  uses-material-design: true
